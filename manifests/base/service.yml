apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: devops-pipeline
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80 # The port that the service will be exposed on
      targetPort: 8080 # The port that the container listens on
  type: LoadBalancer # Use ClusterIP since we’ll expose it via Ingress
